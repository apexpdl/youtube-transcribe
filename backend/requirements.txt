# ---- Core requirements (captions-only capable; safe for Vercel/serverless) ----
# The local Whisper speech-to-text fallback lives in requirements-whisper.txt
# because it pulls in PyTorch, which is too large for serverless platforms.

# ---- Web framework ----
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
python-multipart==0.0.20

# ---- YouTube / transcripts ----
youtube-transcript-api==0.6.2
yt-dlp>=2024.11.18

# ---- Rate limiting ----
slowapi==0.1.9

# ---- Misc ----
httpx==0.28.1
tenacity==9.0.0
aiofiles==24.1.0

# ---- Optional: AI summary / translation (only used when OPENAI_API_KEY is set) ----
openai==1.59.0
